adversary Aa : (Matrix_{n,1}) -> Bool.
bound_adv [
  b <-$ Bool;
  x <-$ Matrix_{n,1};
  y <-$ Matrix_{n,1};
  let m = (b ? x: y);
  b' <- Aa(m);
] : false.
  rnd! x (t -> t + y) (t -> t - y).
norm.  
  norm_nounfold.
  norm.
move A 1.
assumption_decisional! LHL -> t.
norm.
rnd! t (x -> x + u) (x -> x - u).
norm.
norm.
rnd! u (x -> x - t) (x -> x + t).
norm.

norm.